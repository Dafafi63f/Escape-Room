# Dependencias del proyecto (desarrollo, mantenimiento y juego gráfico).
# pygame-ce: pip install -r requirements.txt (import: import pygame).

pandas>=2.0
matplotlib>=3.7
pyinstaller>=6.0
mypy>=1.10
pre-commit>=4.0
pygame-ce>=2.5
